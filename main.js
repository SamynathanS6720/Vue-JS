const app = Vue.createApp({
    data(){
        return{
            // Product
            product: 'Socks',
            // Discription
            discription: 'Good quality socks'
        }
    }
})
// const product = 'Socks'
